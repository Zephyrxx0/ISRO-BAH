---
phase: 04-presentation-dashboard-report
plan: 05
type: execute
wave: 3
depends_on: [02]
files_modified:
  - dashboard/app/star/[ticid]/page.tsx
  - dashboard/components/diagnostic-plot.tsx
  - dashboard/components/parameter-card.tsx
  - dashboard/components/breadcrumb-nav.tsx
autonomous: true
requirements: [DASH-03]

must_haves:
  truths:
    - Star detail page shows 4-panel diagnostic view with pre-rendered images
    - Parameter card displays period, depth, duration, SDE, SNR, Rp/Rs, inclination with uncertainties
    - Breadcrumb shows Home > TIC {id}
    - Interactive Plotly HTML available via iframe toggle with PNG fallback
    - MCMC corner plot and TRICERATOPS+ FPP/NFPP display if available
  artifacts:
    - dashboard/app/star/[ticid]/page.tsx
    - dashboard/components/diagnostic-plot.tsx
    - dashboard/components/parameter-card.tsx
    - dashboard/components/breadcrumb-nav.tsx
  key_links:
    - Page reads per-star JSON from outputs/stars/{ticid}.json
    - Plot images served from public/plots/{ticid}/
    - Layout matches UI-SPEC star detail wireframe (D-15)
    - Graceful degradation per D-04 for non-Gold/non-diagnostic stars
---

<objective>
Implement the per-star diagnostic detail page showing parameter cards, 4-panel diagnostics, MCMC corner plots, and TRICERATOPS+ results, per D-14/D-15 and DASH-03.

Purpose: Gives judges a deep-dive view of individual candidates with all diagnostic evidence supporting classification decisions.
Output: Rich star detail page with parameter display, interactive/static plot toggling, and graceful degradation.
</objective>

<execution_context>
@$HOME/.kiro/get-shit-done/workflows/execute-plan.md
@$HOME/.kiro/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-presentation-dashboard-report/04-CONTEXT.md
@.planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md
@.planning/phases/04-presentation-dashboard-report/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DiagnosticPlot and ParameterCard components</name>
  <files>dashboard/components/diagnostic-plot.tsx, dashboard/components/parameter-card.tsx, dashboard/components/breadcrumb-nav.tsx</files>
  <read_first>
    - .planning/phases/04-presentation-dashboard-report/04-RESEARCH.md (Section 4: Plotly.js Embedding)
    - .planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md (Layout Contract — Star Detail Page, Component Inventory)
    - .planning/phases/04-presentation-dashboard-report/04-CONTEXT.md (D-14, D-15)
  </read_first>
  <action>
1. Create dashboard/components/diagnostic-plot.tsx ('use client'):
   - Props: { pngPath: string, htmlPath: string | null, alt: string }
   - State: showInteractive (boolean, default false)
   - Default view: <img> tag with pngPath, alt text, rounded corners, w-full
   - If htmlPath is not null, show "Interactive" button (top-right overlay, shadcn Button variant="outline" size="sm")
   - When showInteractive=true: render <iframe src={htmlPath}> with className="w-full h-[400px] border-0 rounded"
   - Show "Show Static" button to toggle back
   - If htmlPath is null: no toggle button, just PNG (fallback per D-14)

2. Create dashboard/components/parameter-card.tsx:
   - Props: { params: { period?: number, period_err?: number, depth?: number, depth_err?: number, duration?: number, duration_err?: number, sde: number, snr: number, rp_rs?: number, rp_rs_err?: number, inclination?: number, inclination_err?: number, confidence_tier: string, disposition: string } }
   - Render shadcn Card with bg-[#1e293b]:
     - Grid layout (grid-cols-2 md:grid-cols-4 gap-4) for parameters
     - Each param: label (text-xs text-slate-400) + value ± error (text-lg font-semibold)
     - Format: "1.2749 ± 0.0001 d" for period, "15400 ± 200 ppm" for depth, etc.
     - If value is null/undefined: show "—" in muted text
     - Disposition badge (top-right): colored by confidence_tier per UI-SPEC tier colors
     - Disposition text below badge

3. Create dashboard/components/breadcrumb-nav.tsx:
   - Add shadcn breadcrumb: `npx shadcn@latest add breadcrumb`
   - Props: { ticId: string }
   - Render: Home > TIC {ticId}
   - "Home" links to /
   - Current item (TIC ID) is not a link
  </action>
  <acceptance_criteria>
    - dashboard/components/diagnostic-plot.tsx contains `'use client'`
    - dashboard/components/diagnostic-plot.tsx contains `<iframe` and `<img`
    - dashboard/components/diagnostic-plot.tsx contains `showInteractive`
    - dashboard/components/parameter-card.tsx contains `period_err` or `period ±`
    - dashboard/components/parameter-card.tsx contains `bg-[#1e293b]`
    - dashboard/components/parameter-card.tsx contains `disposition`
    - dashboard/components/breadcrumb-nav.tsx contains `Home` and `TIC`
    - cd dashboard && npx next build exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>DiagnosticPlot supports PNG/iframe toggle, ParameterCard shows all orbital params with uncertainties, BreadcrumbNav renders Home > TIC path</done>
</task>

<task type="auto">
  <name>Task 2: Wire star detail page with full layout</name>
  <files>dashboard/app/star/[ticid]/page.tsx</files>
  <read_first>
    - .planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md (Layout Contract — Star Detail Page)
    - .planning/phases/04-presentation-dashboard-report/04-CONTEXT.md (D-04, D-07, D-14, D-15)
  </read_first>
  <action>
1. Update dashboard/app/star/[ticid]/page.tsx:
   - Keep existing generateStaticParams (from Plan 02)
   - Read star data from outputs/stars/{ticid}.json via fs
   - Page layout per D-15 and UI-SPEC wireframe (top to bottom):

   a) BreadcrumbNav with ticId

   b) ParameterCard with star's parameters

   c) 4-Panel Diagnostics section:
      - Title "Diagnostics" (Heading: 20px, 600 weight)
      - 2×2 grid (grid-cols-1 md:grid-cols-2 gap-4):
        - DiagnosticPlot: pngPath="/plots/{ticid}/raw_detrended.png", htmlPath="/plots/{ticid}/raw_detrended.html", alt="Raw + Detrended Light Curve"
        - DiagnosticPlot: pngPath="/plots/{ticid}/periodogram.png", htmlPath="/plots/{ticid}/periodogram.html", alt="TLS Periodogram"
        - DiagnosticPlot: pngPath="/plots/{ticid}/phase_folded.png", htmlPath="/plots/{ticid}/phase_folded.html", alt="Phase-Folded + Model"
        - DiagnosticPlot: pngPath="/plots/{ticid}/softmax.png", htmlPath="/plots/{ticid}/softmax.html", alt="Classifier Softmax"

   d) MCMC Corner Plot section (conditional):
      - Only render if star data has mcmc_available=true or corner plot path exists
      - DiagnosticPlot: pngPath="/plots/{ticid}/corner.png", htmlPath=null, alt="MCMC Corner Plot"

   e) TRICERATOPS+ section (conditional):
      - Only render if star data has fpp and nfpp fields
      - Display: "FPP: {fpp}" and "NFPP: {nfpp}" with pass/fail indicator (FPP<0.015 = ✓, else ⚠)

   f) Per D-04: if star doesn't have full diagnostics (e.g., non-Gold tier that somehow got a page), show note: "Full diagnostics available for SDE ≥ 7 Gold-tier candidates only."

2. Import DiagnosticPlot dynamically with ssr: false (it uses useState):
   ```tsx
   import dynamic from 'next/dynamic'
   const DiagnosticPlot = dynamic(() => import('@/components/diagnostic-plot'), { ssr: false })
   ```

3. Update sample star JSON in outputs/stars/ to include all expected fields for build testing
  </action>
  <acceptance_criteria>
    - dashboard/app/star/[ticid]/page.tsx contains `<BreadcrumbNav` or `<Breadcrumb`
    - dashboard/app/star/[ticid]/page.tsx contains `<ParameterCard`
    - dashboard/app/star/[ticid]/page.tsx contains `DiagnosticPlot` (4 instances for 4-panel)
    - dashboard/app/star/[ticid]/page.tsx contains `corner.png`
    - dashboard/app/star/[ticid]/page.tsx contains `fpp` and `nfpp`
    - dashboard/app/star/[ticid]/page.tsx contains "Full diagnostics available for SDE"
    - cd dashboard && npx next build exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npx next build 2>&1 | grep -E "(Route|star|Error)" | head -10</automated>
  </verify>
  <done>Star detail page renders full diagnostic layout: breadcrumb, parameter card, 4-panel plots with interactive toggle, conditional MCMC/TRICERATOPS+ sections, graceful degradation note</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Star JSON → page | Build-time fs read; no user input |
| Plotly HTML iframe | Self-hosted pre-rendered HTML from /public/plots/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T (Tampering) | iframe src | accept | Iframes load only from same-origin /plots/ directory; no external URLs |
</threat_model>

<verification>
- cd dashboard && npx next build completes successfully
- out/star/*/index.html files contain diagnostic plot markup
</verification>

<success_criteria>
- Star detail page displays parameter card with all orbital parameters ± uncertainties
- 4-panel diagnostic view shows pre-rendered PNGs with optional interactive toggle
- MCMC corner plot renders when available
- TRICERATOPS+ FPP/NFPP displays with pass/fail indicator
- Graceful degradation for non-Gold stars per D-04
- Breadcrumb navigation works (Home > TIC {id})
</success_criteria>

<output>
After completion, create `.planning/phases/04-presentation-dashboard-report/04-05-SUMMARY.md`
</output>
