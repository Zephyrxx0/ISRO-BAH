---
phase: 04-presentation-dashboard-report
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - dashboard/app/page.tsx
  - dashboard/app/columns.tsx
  - dashboard/components/data-table.tsx
  - dashboard/components/stat-cards.tsx
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - Home page displays filterable/sortable candidate table with all required columns
    - Table columns include TIC ID, period, depth, SDE, classification, disposition, confidence tier
    - Filter bar allows filtering by tier, classification, and period range
    - Each row has "Explore Candidate" CTA linking to /star/[ticid]
    - Summary stat cards display above filter bar
  artifacts:
    - dashboard/app/columns.tsx
    - dashboard/components/data-table.tsx
    - dashboard/components/stat-cards.tsx
  key_links:
    - DataTable reads candidates.json passed as prop from Server Component
    - Tier badges use UI-SPEC colors (Gold=#f59e0b, Silver=#94a3b8, Bronze=#d97706)
    - CTA button text is "Explore Candidate" per UI-SPEC copywriting contract
---

<objective>
Implement the filterable, sortable candidate table on the dashboard home page with stat cards, per D-03 and DASH-02.

Purpose: The candidate table is the primary interface judges interact with — filtering, sorting, and drilling into individual candidates.
Output: Fully functional DataTable with tier badges, sorting, filtering, pagination, and per-row CTA.
</objective>

<execution_context>
@$HOME/.kiro/get-shit-done/workflows/execute-plan.md
@$HOME/.kiro/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-presentation-dashboard-report/04-CONTEXT.md
@.planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md
@.planning/phases/04-presentation-dashboard-report/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create column definitions and DataTable component</name>
  <files>dashboard/app/columns.tsx, dashboard/components/data-table.tsx</files>
  <read_first>
    - .planning/phases/04-presentation-dashboard-report/04-RESEARCH.md (Section 2: shadcn DataTable)
    - .planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md (Component Inventory, Color)
    - .planning/phases/04-presentation-dashboard-report/04-CONTEXT.md (D-03)
  </read_first>
  <action>
1. Install @tanstack/react-table: `cd dashboard && pnpm add @tanstack/react-table`

2. Add shadcn components: `npx shadcn@latest add table badge select input`

3. Create dashboard/app/columns.tsx ('use client'):
   - Define Candidate type: { tic_id: string, period: number, depth: number, sde: number, snr: number, classification: string, disposition: string, confidence_tier: "Gold"|"Silver"|"Bronze", confidence_score: number, duration: number }
   - Column definitions:
     - tic_id: plain text header "TIC ID"
     - period: sortable, cell formatted to 4 decimal places, header "Period (d)"
     - depth: sortable, cell formatted to 1 decimal, header "Depth (ppm)"
     - sde: sortable, cell formatted to 2 decimal, header "SDE"
     - classification: plain text
     - disposition: plain text
     - confidence_tier: cell renders Badge with tier-specific colors:
       Gold → className="bg-[#f59e0b] text-black"
       Silver → className="bg-[#94a3b8] text-black"
       Bronze → className="bg-[#d97706] text-white"
     - actions column: renders Button with text "Explore Candidate" linking to /star/{tic_id}
   - Sortable columns use ArrowUpDown icon from lucide-react in header

4. Create dashboard/components/data-table.tsx ('use client'):
   - Generic DataTable<TData, TValue> component accepting columns + data props
   - State: sorting (SortingState), columnFilters (ColumnFiltersState)
   - useReactTable with getCoreRowModel, getSortedRowModel, getFilteredRowModel, getPaginationRowModel
   - Filter bar (flex row with gap-4):
     - Select for tier filter (All / Gold / Silver / Bronze) → sets confidence_tier column filter
     - Select for classification filter (All / Planet Candidate / Eclipsing Binary / Background Blend / Stellar Variability) → sets classification column filter
     - Two Input fields for period range (Min / Max) → custom range filter on period column
   - Table rendering using shadcn Table, TableHeader, TableRow, TableHead, TableBody, TableCell
   - Pagination: Previous/Next buttons with page count display, 20 rows per page
   - Empty state: "No candidates match your filters" per UI-SPEC copywriting
  </action>
  <acceptance_criteria>
    - dashboard/app/columns.tsx contains `'use client'`
    - dashboard/app/columns.tsx contains `ColumnDef<Candidate>[]`
    - dashboard/app/columns.tsx contains `bg-[#f59e0b]` (Gold badge color)
    - dashboard/app/columns.tsx contains `Explore Candidate`
    - dashboard/components/data-table.tsx contains `useReactTable`
    - dashboard/components/data-table.tsx contains `getPaginationRowModel`
    - dashboard/components/data-table.tsx contains `getFilteredRowModel`
    - dashboard/components/data-table.tsx contains `No candidates match your filters`
    - cd dashboard && npx next build exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>DataTable component renders sortable/filterable table with tier badges and per-row CTA, builds without error</done>
</task>

<task type="auto">
  <name>Task 2: Create stat cards and wire home page</name>
  <files>dashboard/components/stat-cards.tsx, dashboard/app/page.tsx</files>
  <read_first>
    - .planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md (Layout Contract — Home Page)
    - .planning/phases/04-presentation-dashboard-report/04-CONTEXT.md (D-03)
  </read_first>
  <action>
1. Add shadcn Card: `npx shadcn@latest add card`

2. Create dashboard/components/stat-cards.tsx ('use client'):
   - Props: { totalCandidates: number, goldCount: number, planetCount: number, avgSde: number }
   - Render 4 Card components in a responsive grid (grid-cols-2 md:grid-cols-4 gap-4):
     - "Total Candidates" — totalCandidates value
     - "Gold Tier" — goldCount value, text-[#f59e0b]
     - "Planet Candidates" — planetCount value
     - "Avg SDE" — avgSde formatted to 1 decimal
   - Card styling: bg-[#1e293b] border-slate-700 per UI-SPEC secondary color
   - Card title: text-xs text-slate-400 uppercase (Label role from UI-SPEC: 12px, 600 weight)
   - Card value: text-2xl font-semibold text-slate-100

3. Update dashboard/app/page.tsx:
   - Server Component (no 'use client')
   - Reads outputs/candidates.json via fs.readFileSync at build time
   - Computes stats: total count, gold count, planet count (disposition === "PLANET CANDIDATE"), avg SDE
   - Renders StatCards with computed stats
   - Renders DataTable with candidates data and column definitions
   - Page title: "Exoplanet Candidates" as h1 (Display role: 32px, 600 weight)
   - Layout per UI-SPEC: stat cards → filter bar (inside DataTable) → table
  </action>
  <acceptance_criteria>
    - dashboard/components/stat-cards.tsx contains `Total Candidates` and `Gold Tier` and `Planet Candidates` and `Avg SDE`
    - dashboard/components/stat-cards.tsx contains `bg-[#1e293b]`
    - dashboard/app/page.tsx contains `fs.readFileSync`
    - dashboard/app/page.tsx contains `candidates.json`
    - dashboard/app/page.tsx does NOT contain `'use client'`
    - dashboard/app/page.tsx contains `<StatCards` and `<DataTable`
    - cd dashboard && npx next build exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Home page displays 4 stat cards above a filterable/sortable candidate table, reading data from outputs/candidates.json at build time</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidates.json → DataTable | Build-time data, not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | I (Info Disclosure) | candidates.json | accept | All candidate data is intentionally public scientific output |
</threat_model>

<verification>
- cd dashboard && npx next build completes successfully
- Home page (out/index.html) contains table markup with candidate data
</verification>

<success_criteria>
- DataTable displays all DASH-02 columns: TIC ID, period, depth, SDE, classification, disposition, confidence tier
- Filtering works for tier and classification
- Sorting works on numeric columns (period, depth, SDE)
- Pagination shows 20 rows per page
- "Explore Candidate" CTA links to /star/[ticid]
- Stat cards show summary counts per D-03
</success_criteria>

<output>
After completion, create `.planning/phases/04-presentation-dashboard-report/04-03-SUMMARY.md`
</output>
