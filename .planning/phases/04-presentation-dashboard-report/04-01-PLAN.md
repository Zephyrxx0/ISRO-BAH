---
phase: 04-presentation-dashboard-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/report/generate_csv.py
  - src/report/generate_pdf.py
  - src/report/generate_dashboard_data.py
  - outputs/.gitkeep
autonomous: true
requirements: [RPRT-01, RPRT-02, RPRT-03]

must_haves:
  truths:
    - generate_csv.py produces catalogue.csv with all 17 required columns
    - generate_pdf.py produces 4-page report.pdf from pre-rendered PNGs
    - generate_dashboard_data.py produces candidates.json and per-star JSON files
  artifacts:
    - src/report/generate_csv.py
    - src/report/generate_pdf.py
    - src/report/generate_dashboard_data.py
  key_links:
    - CSV disposition labels match RPRT-03 exactly
    - PDF pages match RPRT-01 layout (Methodology, Results, Validation, Uncertainties)
    - candidates.json schema matches what dashboard DataTable expects
---

<objective>
Create the Python-side data generation scripts that produce all Phase 4 deliverables: candidate catalogue CSV, 4-page PDF report, and dashboard JSON data files.

Purpose: These scripts run as the final step of run_pipeline.py, consuming Phase 3 outputs and producing the files that both judges and the Next.js dashboard consume.
Output: Three Python modules in src/report/ that generate CSV, PDF, and JSON outputs.
</objective>

<execution_context>
@$HOME/.kiro/get-shit-done/workflows/execute-plan.md
@$HOME/.kiro/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-presentation-dashboard-report/04-CONTEXT.md
@.planning/phases/04-presentation-dashboard-report/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create candidate catalogue CSV generator</name>
  <files>src/report/generate_csv.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md (RPRT-02, RPRT-03 sections)
    - .planning/phases/04-presentation-dashboard-report/04-RESEARCH.md (Section 6: Candidate Catalogue CSV)
  </read_first>
  <action>
Create src/report/generate_csv.py with:

1. Define COLUMNS list with exactly 17 columns in this order: tic_id, period, depth, duration, sde, snr, classification, disposition, confidence_score, confidence_tier, period_err, depth_err, duration_err, rp_rs, rp_rs_err, inclination, inclination_err

2. Define DISPOSITIONS constant with exactly 5 labels: "PLANET CANDIDATE", "ECLIPSING BINARY", "BACKGROUND BLEND", "STELLAR VARIABILITY", "SUB-THRESHOLD"

3. Function `assign_disposition(classification: str, sde: float) -> str`:
   - If sde < 5: return "SUB-THRESHOLD"
   - Map classification labels: PC → PLANET CANDIDATE, EB → ECLIPSING BINARY, Blend → BACKGROUND BLEND, StellarVar → STELLAR VARIABILITY
   - Default: SUB-THRESHOLD

4. Function `generate_catalogue(results_dir: Path, output_path: Path) -> pd.DataFrame`:
   - Read candidate results from results_dir (expects Parquet or JSON from Phase 3)
   - Apply assign_disposition to each row
   - Validate all dispositions are in DISPOSITIONS set (raise ValueError if not)
   - Sort by SDE descending
   - Write to CSV with float_format='%.6f'
   - Return DataFrame

5. Add `if __name__ == '__main__'` block accepting --results-dir and --output-path args via argparse
  </action>
  <acceptance_criteria>
    - src/report/generate_csv.py contains `COLUMNS = [` with exactly 17 entries
    - src/report/generate_csv.py contains `DISPOSITIONS = [` with "PLANET CANDIDATE", "ECLIPSING BINARY", "BACKGROUND BLEND", "STELLAR VARIABILITY", "SUB-THRESHOLD"
    - src/report/generate_csv.py contains `def generate_catalogue(`
    - src/report/generate_csv.py contains `def assign_disposition(`
    - src/report/generate_csv.py contains `df.to_csv(` with `float_format`
    - python -c "import src.report.generate_csv" exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.report.generate_csv import generate_catalogue, assign_disposition, COLUMNS, DISPOSITIONS; assert len(COLUMNS)==17; assert len(DISPOSITIONS)==5"</automated>
  </verify>
  <done>generate_csv.py importable with correct column/disposition constants and both public functions defined</done>
</task>

<task type="auto">
  <name>Task 2: Create 4-page PDF report generator</name>
  <files>src/report/generate_pdf.py</files>
  <read_first>
    - .planning/REQUIREMENTS.md (RPRT-01 section)
    - .planning/phases/04-presentation-dashboard-report/04-RESEARCH.md (Section 5: PDF Report Generation)
    - .planning/phases/04-presentation-dashboard-report/04-CONTEXT.md (D-09, D-10, D-11)
  </read_first>
  <action>
Create src/report/generate_pdf.py with:

1. Function `generate_report(outputs_dir: Path, output_path: Path) -> None` using matplotlib PdfPages

2. Page 1 — Methodology: embed pipeline_flowchart.png + confusion_matrix.png. Title "Methodology". Per D-10, reuse existing PNGs from Phase 3.

3. Page 2 — Results: read summary_stats.json for candidate counts, embed best_planet_phase_folded.png. Title "Results".

4. Page 3 — Validation: embed triceratops_fpp.png + completeness_map.png. Add recovery test results text (WASP-121b, TOI-270, L 98-59, TOI-700d). Title "Validation".

5. Page 4 — Uncertainties: embed best_planet_corner.png. Add assumptions/limitations text list. Title "Uncertainties & Limitations".

6. Per D-11, guard every mpimg.imread with Path.exists() check. If file missing, show "Not computed" placeholder text in gray. Report ALWAYS generates even with partial data.

7. Set PDF metadata via infodict: Title='ISRO BAH 2026 PS-07: AI-Enabled Exoplanet Detection'

8. Figure size: (8.5, 11) for letter-size pages

9. Add `if __name__ == '__main__'` block with --outputs-dir and --output-path args
  </action>
  <acceptance_criteria>
    - src/report/generate_pdf.py contains `from matplotlib.backends.backend_pdf import PdfPages`
    - src/report/generate_pdf.py contains `def generate_report(`
    - src/report/generate_pdf.py contains `_page_methodology` and `_page_results` and `_page_validation` and `_page_uncertainties`
    - src/report/generate_pdf.py contains `.exists()` guard before every mpimg.imread call
    - src/report/generate_pdf.py contains `figsize=(8.5, 11)`
    - src/report/generate_pdf.py contains `infodict`
    - python -c "import src.report.generate_pdf" exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.report.generate_pdf import generate_report; import inspect; src=inspect.getsource(generate_report); assert 'PdfPages' in src"</automated>
  </verify>
  <done>generate_pdf.py produces a 4-page PDF from existing pipeline PNGs with graceful fallback for missing files</done>
</task>

<task type="auto">
  <name>Task 3: Create dashboard JSON data generator</name>
  <files>src/report/generate_dashboard_data.py</files>
  <read_first>
    - .planning/phases/04-presentation-dashboard-report/04-CONTEXT.md (D-06, D-07)
    - .planning/phases/04-presentation-dashboard-report/04-RESEARCH.md (Section 7: Integration)
  </read_first>
  <action>
Create src/report/generate_dashboard_data.py with:

1. Function `generate_candidates_json(results_dir: Path, output_path: Path) -> None`:
   - Read candidate results from Phase 3 outputs
   - Select columns: tic_id, period, depth, sde, snr, classification, disposition, confidence_score, confidence_tier, duration
   - Write as JSON array to output_path (candidates.json)
   - Per D-06: this powers the filterable table on the dashboard home page

2. Function `generate_star_jsons(results_dir: Path, output_dir: Path) -> int`:
   - Per D-07: only SDE≥7 candidates get per-star JSON files
   - For each SDE≥7 candidate, create output_dir/{tic_id}.json containing:
     - All candidate fields (period, depth, duration, sde, snr, classification, disposition, confidence_score, confidence_tier)
     - Parameter estimates (rp_rs, inclination, period_err, depth_err, etc.) if available
     - Plot paths (diagnostic_4panel.png, periodogram.html, phase_folded.html, softmax.html, corner.png)
     - TRICERATOPS+ results (fpp, nfpp) if available
     - RA and Dec coordinates (for star map)
   - Return count of star JSONs written

3. Add `if __name__ == '__main__'` block with --results-dir, --output-dir, --candidates-output args
  </action>
  <acceptance_criteria>
    - src/report/generate_dashboard_data.py contains `def generate_candidates_json(`
    - src/report/generate_dashboard_data.py contains `def generate_star_jsons(`
    - src/report/generate_dashboard_data.py contains `sde >= 7` or `sde > 6` filter for star JSONs
    - src/report/generate_dashboard_data.py contains `json.dump` or `Path.write_text`
    - python -c "import src.report.generate_dashboard_data" exits 0
  </acceptance_criteria>
  <verify>
    <automated>python -c "from src.report.generate_dashboard_data import generate_candidates_json, generate_star_jsons"</automated>
  </verify>
  <done>Dashboard data generator creates candidates.json (all SDE≥5) and per-star JSONs (SDE≥7 only) from pipeline outputs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline outputs → report scripts | File paths from pipeline; no user input |
| Report outputs → filesystem | PDF/CSV/JSON written to local outputs/ dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | outputs/ dir | accept | Local-only pipeline, no external input; outputs gitignored |
| T-04-02 | D (DoS) | PDF generation | accept | Pipeline runs locally with bounded data (~500 candidates max) |
</threat_model>

<verification>
- python -c "import src.report.generate_csv, src.report.generate_pdf, src.report.generate_dashboard_data" exits 0
- All three modules have public entry-point functions callable with Path arguments
</verification>

<success_criteria>
- CSV generator handles all 17 columns + 5 dispositions per RPRT-02/RPRT-03
- PDF generator produces 4 pages matching RPRT-01 layout with graceful fallback
- Dashboard data generator produces candidates.json + per-star JSONs per D-06/D-07
</success_criteria>

<output>
After completion, create `.planning/phases/04-presentation-dashboard-report/04-01-SUMMARY.md`
</output>
