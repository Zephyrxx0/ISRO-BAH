---
phase: 04-presentation-dashboard-report
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/package.json
  - dashboard/next.config.js
  - dashboard/tailwind.config.ts
  - dashboard/app/layout.tsx
  - dashboard/app/page.tsx
  - dashboard/app/star/[ticid]/page.tsx
  - dashboard/app/map/page.tsx
  - dashboard/components/navbar.tsx
  - dashboard/components/ui/
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - next build produces static export in dashboard/out/
    - App Router routes exist for /, /star/[ticid], /map
    - Navbar with Candidates, Star Map, About links renders on all pages
    - Dark theme (slate-900 background) applied globally
  artifacts:
    - dashboard/package.json
    - dashboard/next.config.js
    - dashboard/app/layout.tsx
    - dashboard/app/page.tsx
    - dashboard/app/star/[ticid]/page.tsx
    - dashboard/app/map/page.tsx
    - dashboard/components/navbar.tsx
  key_links:
    - next.config.js has output:'export' and images:{unoptimized:true}
    - Layout uses Inter font and dark slate-900 background per UI-SPEC
    - Navbar matches D-02 (Candidates, Star Map, About)
---

<objective>
Scaffold the Next.js 15 dashboard with App Router, shadcn/ui, Tailwind 4, dark theme, and route structure matching the UI-SPEC layout contract.

Purpose: Establishes the frontend foundation that all subsequent dashboard plans build upon.
Output: Working Next.js static site skeleton with routing, navigation, and design tokens.
</objective>

<execution_context>
@$HOME/.kiro/get-shit-done/workflows/execute-plan.md
@$HOME/.kiro/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-presentation-dashboard-report/04-CONTEXT.md
@.planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md
@.planning/phases/04-presentation-dashboard-report/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 15 project with static export config</name>
  <files>dashboard/package.json, dashboard/next.config.js, dashboard/tsconfig.json, dashboard/app/layout.tsx, dashboard/app/globals.css</files>
  <read_first>
    - .planning/phases/04-presentation-dashboard-report/04-RESEARCH.md (Section 1: Next.js 15 App Router)
    - .planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md (Design System, Color, Typography sections)
  </read_first>
  <action>
1. Run `npx create-next-app@latest dashboard --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"` (answer defaults)

2. Update dashboard/next.config.js:
```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  trailingSlash: true,
}
module.exports = nextConfig
```

3. Update dashboard/app/layout.tsx:
   - Import Inter font from next/font/google
   - Set metadata: title="ISRO Exoplanet Detection Pipeline", description="AI-enabled transit detection dashboard"
   - Apply dark theme: body className with bg-[#0f172a] text-slate-100 font-sans
   - Include Navbar component (import from @/components/navbar)

4. Update dashboard/app/globals.css with Tailwind 4 imports and dark theme CSS variables:
   - --background: #0f172a (dominant)
   - --card: #1e293b (secondary)
   - --accent: #f59e0b (amber)
   - --foreground: #f1f5f9 (slate-100)

5. Pin dependencies in package.json: next@15.1.0, react@^19.0.0, react-dom@^19.0.0
  </action>
  <acceptance_criteria>
    - dashboard/next.config.js contains `output: 'export'`
    - dashboard/next.config.js contains `images: { unoptimized: true }`
    - dashboard/app/layout.tsx contains `Inter` font import
    - dashboard/app/layout.tsx contains `bg-[#0f172a]`
    - dashboard/app/globals.css contains `--background` and `--accent`
    - dashboard/package.json contains `"next": "15` version
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js 15 project builds successfully with static export, dark theme, and Inter font</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and create Navbar component</name>
  <files>dashboard/components.json, dashboard/components/ui/, dashboard/components/navbar.tsx</files>
  <read_first>
    - .planning/phases/04-presentation-dashboard-report/04-UI-SPEC.md (Component Inventory, Layout Contract)
    - .planning/phases/04-presentation-dashboard-report/04-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
1. Run `cd dashboard && npx shadcn@latest init` (accept defaults for dark theme)

2. Add shadcn components needed for this plan: `npx shadcn@latest add navigation-menu button`

3. Create dashboard/components/navbar.tsx:
   - 'use client' directive
   - Fixed top navbar with bg-[#1e293b] border-b border-slate-700
   - Left: project title "Exoplanet Pipeline" (text, not logo)
   - Center/right: NavigationMenu with 3 links per D-02:
     - "Candidates" → / (active state: text-amber-500 underline)
     - "Star Map" → /map
     - "About" → /about
   - Use Inter font, 14px (text-sm), weight 400
   - Height: 64px (consistent with UI-SPEC xl=32px × 2 for centered content)
   - Use `usePathname()` from next/navigation for active state highlighting

4. Ensure layout.tsx imports and renders <Navbar /> above {children}
  </action>
  <acceptance_criteria>
    - dashboard/components.json exists
    - dashboard/components/navbar.tsx contains `'use client'`
    - dashboard/components/navbar.tsx contains `Candidates` and `Star Map` and `About`
    - dashboard/components/navbar.tsx contains `usePathname`
    - dashboard/components/navbar.tsx contains links to `/`, `/map`, `/about`
    - dashboard/app/layout.tsx contains `<Navbar`
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>shadcn initialized, Navbar renders with 3 navigation links per D-02, active state highlighting works</done>
</task>

<task type="auto">
  <name>Task 3: Create route stubs for all pages</name>
  <files>dashboard/app/page.tsx, dashboard/app/star/[ticid]/page.tsx, dashboard/app/map/page.tsx, dashboard/app/about/page.tsx</files>
  <read_first>
    - .planning/phases/04-presentation-dashboard-report/04-CONTEXT.md (D-01, D-04, D-07)
  </read_first>
  <action>
1. dashboard/app/page.tsx (home):
   - Server Component that reads outputs/candidates.json via fs at build time
   - Renders placeholder text "Candidate Table" (actual DataTable in Plan 03)
   - Export generateStaticParams is NOT needed here (static route, no params)

2. dashboard/app/star/[ticid]/page.tsx:
   - Server Component
   - Export `generateStaticParams()` that reads outputs/stars/ directory, returns {ticid} for each .json file
   - Reads star data from outputs/stars/{ticid}.json
   - Renders placeholder "Star Detail: {ticid}" (actual implementation in Plan 05)
   - Per D-07: only SDE≥7 stars have JSON files, so only those get pages
   - Note: In Next.js 15, params is a Promise — use `const { ticid } = await params`

3. dashboard/app/map/page.tsx:
   - Renders placeholder "Star Map" (actual Leaflet in Plan 04)

4. dashboard/app/about/page.tsx:
   - Static page with project description: "AI-Enabled Exoplanet Detection Pipeline — ISRO BAH 2026 PS-07"
   - Brief methodology summary (3-4 sentences)

5. Create dashboard/outputs/ directory with sample candidates.json (3 fake entries) and outputs/stars/ with 1 sample star JSON for build validation. Add .gitkeep.
  </action>
  <acceptance_criteria>
    - dashboard/app/star/[ticid]/page.tsx contains `generateStaticParams`
    - dashboard/app/star/[ticid]/page.tsx contains `await params`
    - dashboard/app/star/[ticid]/page.tsx contains `fs.readFileSync` or `fs.readdirSync`
    - dashboard/app/map/page.tsx exists
    - dashboard/app/about/page.tsx exists
    - dashboard/outputs/candidates.json exists with valid JSON array
    - cd dashboard && npx next build exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd dashboard && npx next build 2>&1 | grep -E "(Route|✓|Error)" | head -10</automated>
  </verify>
  <done>All 4 routes build successfully, generateStaticParams enumerates star pages from filesystem, static export completes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outputs/ JSON → dashboard | Pipeline-generated JSON read at build time; no runtime user input |
| Static HTML → judge browser | Read-only static assets served to judges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | I (Info Disclosure) | Static export | accept | Dashboard is intentionally public read-only; no secrets in outputs |
| T-04-04 | T (Tampering) | outputs/ JSON | accept | Build-time only; tampered JSON = bad build, not runtime exploit |
</threat_model>

<verification>
- cd dashboard && npx next build completes with 0 exit code
- out/ directory contains index.html, star/*/index.html, map/index.html, about/index.html
</verification>

<success_criteria>
- Next.js 15 static site builds with App Router routes for /, /star/[ticid], /map, /about
- Dark theme applied globally (bg-[#0f172a])
- Navbar renders on all pages with correct links
- generateStaticParams reads filesystem for star pages
</success_criteria>

<output>
After completion, create `.planning/phases/04-presentation-dashboard-report/04-02-SUMMARY.md`
</output>
